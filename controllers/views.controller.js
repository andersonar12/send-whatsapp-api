exports.signin = (req, res, next) => {
  // mostrar template con signin
  res.status(200).render('signin');

  // El template debe tener un boton

  // al dar click al boton tomar captura a QR

  // Al obtener el QR, insertar imagen en HTML con la captura QR
}